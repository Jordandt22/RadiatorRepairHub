/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["lh3.googleusercontent.com", "picsum.photos"], // ✅ add allowed domains here
  },
};

export default nextConfig;
