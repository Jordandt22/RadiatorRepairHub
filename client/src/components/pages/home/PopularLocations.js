import PopularLocationsContent from "./PopularLocationsContent";

function PopularLocations({ states = [] }) {
  return <PopularLocationsContent states={states} />;
}

export default PopularLocations;
